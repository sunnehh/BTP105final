#ifndef _FUNDAMENTALS_H
#define _FUNDAMENTALS_H
#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define TRUE 1


/*
* Program which asks user for a string input in stdin and an index position, then displays the character at the given index.
* Takes no parameters and has no return type.
*/
void fundamentals();

#endif